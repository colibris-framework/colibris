[pycodestyle]
max-line-length = 120
ignore = E129,W504

[testenv]
changedir = tests
deps = pytest
       requests
commands = pytest -v {posargs} -p aiohttp.pytest_plugin

[pytest]
norecursedirs = dummy-skeleton
testpaths = tests
